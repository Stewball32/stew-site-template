package main

// TODO: Application entry point.
// Creates PocketBase app instance.
// In OnServe hook: registers schemas, hooks, routes, starts WebSocket Hub, starts Disgo bot (non-blocking).
// Registers OnTerminate hook for Disgo cleanup via Close(ctx).
// Calls app.Start() — blocking.

func main() {
	// TODO: implement startup sequence
}
