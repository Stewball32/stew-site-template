package routes

// TODO: Custom HTTP route registration.
// RegisterRoutes(app, hub) registers all custom endpoints on PocketBase's ServeMux via OnServe.
// Mounts the WebSocket upgrade handler at /api/ws (delegating to internal/websocket).
// Binds auth/role middleware to protected endpoints.
