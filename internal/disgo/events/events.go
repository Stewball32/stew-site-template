package events

// TODO: Discord gateway event listeners for non-interaction events.
// RegisterListeners(client) attaches listeners via client.EventManager().AddEventListeners().
// Handles events like MessageCreate, GuildMemberJoin, etc.
// Each event domain can be split into its own file (e.g., messages.go, members.go).
