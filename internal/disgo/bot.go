package disgo

// TODO: Discord bot client setup.
// NewBot() creates and configures the Disgo client, registers slash commands, and attaches event listeners.
// OpenGateway(ctx) connects to Discord — non-blocking, runs in background goroutines.
// Close(ctx) shuts down the gateway connection cleanly.
// The bot client is shared with PocketBase hooks for sending notifications.
